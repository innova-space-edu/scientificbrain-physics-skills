# Evaluations

`routing-cases.json` provides the first positive and negative routing cases. Future releases should add per-skill execution/correctness tests, security checks, and benchmark artifacts following the general structure used by NVIDIA Agent Skills.

The v0.1.0 gate is structural: all 15 skill directories require `SKILL.md` and `skill-card.md`, and the Python helper scripts must compile.
